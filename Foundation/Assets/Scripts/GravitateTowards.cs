using UnityEngine;
using System.Collections;

public class GravitateTowards : MonoBehaviour {
	//public GameObject gravityObject;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//float oGrav = gravityObject.transform.GetComponent("FauxGravity").gravity;
		/*Vector3 gravityUp = gravityObject.transform.position - transform.position;
    			gravityUp.Normalize();

		this.rigidbody.AddForce (gravityUp * -5f);*/
	}
}
